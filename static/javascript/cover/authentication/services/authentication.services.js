/**
 * Created by shahab on 15-09-04.
 */
(function(){

    angular
        .module('cover.authentication.services')
        .factory('authentication', authentication);

    authentication.$inject = ['$resource'];

    function authentication(){

    }
})();